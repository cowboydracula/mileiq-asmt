/**
 * MileIQ - Login Component ** COMMENTED OUT EXAMPLE **
 * refrained from using Cucumber considering
 * these are TDD, not BDD, tests
 */

/*
import WLOGLoginComponent from '@/components/WLOGLoginComponent.ts';
describe('WLOG - Login Component', () => {
    beforeEach(() => {
      cy.mount(`<WLOGLoginComponent></WLOGLoginComponent>`);
    });
  
    it('WLOG - should render login form', () => {
      cy.get('form#login-form').should('exist');
      cy.get('input[name="username"]').should('exist');
      cy.get('input[name="password"]').should('exist');
      cy.get('button[type="submit"]').should('exist');
    });
  
    // cont.
  });
*/