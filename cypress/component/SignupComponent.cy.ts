/**
 * MileIQ - Signup Component ** COMMENTED OUT EXAMPLE**
 * refrained from using Cucumber considering
 * these are TDD, not BDD, tests
 */

/*
import WLOGSignupComponent from '@/components/WLOGSignupComponent.ts';

describe('WLOG - Signup Component', () => {
    beforeEach(() => {
      cy.mount(`<WLOGSignupComponent></WLOGSignupComponent>`);
    });
  
    it('WLOG - signup form displays', () => {
      cy.get('form#signup-form').should('exist');
      cy.get('input[name="username"]').should('exist');
      cy.get('input[name="password"]').should('exist');
      cy.get('input[name="email"]').should('exist');
      cy.get('button[type="submit"]').should('exist');
    });
  
    // cont.
  });
*/